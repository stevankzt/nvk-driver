# 🚀 Инструкция по деплою на Render.com

## Шаг 1: Подготовка Git репозитория

### 1.1 Инициализация Git (если еще не сделано)

Откройте PowerShell в папке проекта и выполните:

```powershell
git init
git add .
git commit -m "Initial commit - NVK-Driver app"
```

### 1.2 Создание GitHub репозитория

1. Откройте https://github.com/new
2. Введите название: `nvk-driver`
3. Описание: `Telegram Mini App для студенческого трансфера`
4. Выберите **Public** (или Private)
5. НЕ создавайте README, .gitignore и лицензию (у нас уже есть)
6. Нажмите **Create repository**

### 1.3 Загрузка на GitHub

GitHub покажет команды. Выполните в PowerShell:

```powershell
git remote add origin https://github.com/ВАШ_USERNAME/nvk-driver.git
git branch -M main
git push -u origin main
```

---

## Шаг 2: Деплой на Render.com

### 2.1 Регистрация на Render

1. Откройте https://render.com
2. Нажмите **Get Started for Free**
3. Войдите через **GitHub** (это самый простой способ)
4. Разрешите доступ Render к GitHub

### 2.2 Создание Web Service

1. На дашборде Render нажмите **New +** → **Web Service**
2. Выберите **Connect a repository**
3. Найдите и выберите репозиторий `nvk-driver`
4. Нажмите **Connect**

### 2.3 Настройка сервиса

Заполните следующие поля:

- **Name:** `nvk-driver` (или любое имя)
- **Region:** `Frankfurt (EU Central)` (ближе к России)
- **Branch:** `main`
- **Root Directory:** оставьте пустым
- **Runtime:** `Node`
- **Build Command:** `npm install`
- **Start Command:** `npm start`
- **Instance Type:** `Free`

### 2.4 Добавление переменных окружения

Прокрутите вниз до раздела **Environment Variables** и нажмите **Add Environment Variable**.

Добавьте следующие переменные:

| Key | Value |
|-----|-------|
| `BOT_TOKEN` | `8562445737:AAGFhm5Kbwgt256zNi0e4RyYN7kfTaL9Jx0` |
| `BOT_USERNAME` | `nvk_driver_bot` |
| `APP_SHORT_NAME` | `nvkdriver` |
| `PORT` | `3000` |
| `NODE_ENV` | `production` |

⚠️ **ВАЖНО:** Для переменной `APP_URL` НЕ вводите значение сейчас. Мы добавим его после деплоя.

### 2.5 Запуск деплоя

1. Нажмите **Create Web Service** внизу страницы
2. Render начнет деплой (займет 2-5 минут)
3. Дождитесь статуса **Live** (зеленая галочка)

---

## Шаг 3: Получение URL приложения

После успешного деплоя:

1. Вверху страницы вы увидите URL вида: `https://nvk-driver.onrender.com`
2. Скопируйте этот URL

### 3.1 Добавление APP_URL

1. На странице вашего сервиса в Render перейдите на вкладку **Environment**
2. Нажмите **Add Environment Variable**
3. Добавьте:
   - **Key:** `APP_URL`
   - **Value:** `https://nvk-driver.onrender.com` (ваш URL)
4. Нажмите **Save Changes**
5. Render автоматически перезапустит сервис

---

## Шаг 4: Обновление BotFather

1. Откройте @BotFather в Telegram
2. Отправьте `/myapps`
3. Выберите ваше приложение **nvkdriver**
4. Нажмите **Edit Web App URL**
5. Вставьте ваш Render URL: `https://nvk-driver.onrender.com`
6. Подтвердите изменения

---

## Шаг 5: Тестирование

1. Откройте вашего бота @nvk_driver_bot в Telegram
2. Напишите `/start`
3. Нажмите **🚀 Открыть приложение**
4. Приложение должно открыться и работать! 🎉

---

## 🔧 Обновление приложения

Когда вы внесете изменения в код:

```powershell
git add .
git commit -m "Описание изменений"
git push
```

Render автоматически задеплоит новую версию!

---

## 📊 Мониторинг

На Render вы можете:
- Смотреть логи в реальном времени
- Перезапускать сервис
- Смотреть метрики использования

---

## ⚠️ Важные замечания

### Холодный старт
Бесплатный план Render "засыпает" после 15 минут неактивности. Первый запрос может занять 30-60 секунд.

### Решение:
Добавьте внешний мониторинг (например, UptimeRobot) для пинга каждые 5-10 минут.

### Ограничения бесплатного плана:
- 750 часов работы в месяц (достаточно для одного сервиса)
- Засыпание после неактивности
- Ограниченные ресурсы

---

## 🆘 Решение проблем

### Ошибка деплоя
- Проверьте логи в Render Dashboard → Logs
- Убедитесь, что все зависимости в package.json

### Бот не отвечает
- Проверьте переменную BOT_TOKEN
- Убедитесь, что сервис в статусе Live

### База данных пустая
- Это нормально! База создастся автоматически при первой поездке

---

## ✅ Чек-лист деплоя

- [ ] Код загружен на GitHub
- [ ] Сервис создан на Render
- [ ] Все переменные окружения добавлены
- [ ] Статус сервиса: Live
- [ ] URL обновлен в BotFather
- [ ] Бот отвечает на /start
- [ ] Приложение открывается в Telegram

---

**После завершения всех шагов ваше приложение будет доступно 24/7!** 🎉

URL будет постоянным и не изменится.
