# 🚀 Инструкция по запуску NVK-Driver

## 📋 Оглавление
1. [Установка зависимостей](#установка-зависимостей)
2. [Запуск приложения](#запуск-приложения)
3. [Тестирование локально](#тестирование-локально)
4. [Деплой на хостинг](#деплой-на-хостинг)
5. [Решение проблем](#решение-проблем)

---

## 1. Установка зависимостей

### Шаг 1: Откройте PowerShell в папке проекта

Нажмите `Shift + ПКМ` в папке проекта и выберите "Открыть окно PowerShell здесь"

### Шаг 2: Установите все необходимые пакеты

```powershell
npm install
```

Это установит все зависимости:
- `express` - веб-сервер
- `cors` - для работы с API
- `dotenv` - для переменных окружения
- `better-sqlite3` - база данных
- `node-telegram-bot-api` - интеграция с Telegram
- `nodemon` - автоперезагрузка при разработке

⏳ Установка займет 1-2 минуты

---

## 2. Запуск приложения

### Запуск в режиме разработки (с автоперезагрузкой):

```powershell
npm run dev
```

### Запуск в обычном режиме:

```powershell
npm start
```

После запуска вы увидите:
```
╔═══════════════════════════════════════════╗
║                                           ║
║        🚗 NVK-Driver Server 🚗           ║
║                                           ║
║  ✅ Server running on port 3000          ║
║  ✅ Database initialized                  ║
║  ✅ Telegram bot connected                ║
║                                           ║
║  📱 Open: http://localhost:3000          ║
║                                           ║
╚═══════════════════════════════════════════╝
```

---

## 3. Тестирование локально

### Вариант А: Тестирование в браузере

1. Откройте браузер
2. Перейдите на `http://localhost:3000`
3. Вы увидите интерфейс приложения
4. Можете тестировать все функции (кроме Telegram интеграции)

### Вариант Б: Тестирование через Telegram (требует публичный URL)

Для этого нужно использовать **ngrok** для создания туннеля:

#### Установка ngrok:

1. Скачайте с https://ngrok.com/download
2. Распакуйте в любую папку
3. Запустите в отдельном терминале:

```powershell
.\ngrok.exe http 3000
```

4. Скопируйте URL вида `https://xxxx-xxx-xxx-xxx-xxx.ngrok-free.app`

#### Обновление настроек:

1. Откройте файл `.env`
2. Измените `APP_URL` на ваш ngrok URL:
```
APP_URL=https://xxxx-xxx-xxx-xxx-xxx.ngrok-free.app
```
3. Перезапустите сервер

#### Обновление Web App URL в BotFather:

1. Откройте @BotFather в Telegram
2. Отправьте `/myapps`
3. Выберите ваше приложение
4. Выберите "Edit Web App URL"
5. Вставьте ваш ngrok URL

Теперь можно тестировать через Telegram:
- Откройте вашего бота в Telegram
- Напишите `/start`
- Нажмите "Открыть приложение"

---

## 4. Деплой на хостинг

### Рекомендуемые варианты хостинга:

#### Вариант 1: Render.com (Бесплатно)

1. Создайте аккаунт на https://render.com
2. Нажмите "New +" → "Web Service"
3. Подключите ваш GitHub репозиторий
4. Настройки:
   - **Build Command:** `npm install`
   - **Start Command:** `npm start`
   - **Environment Variables:** Добавьте все из `.env` файла
5. Нажмите "Create Web Service"
6. Скопируйте URL вашего приложения
7. Обновите в BotFather через `/myapps`

#### Вариант 2: Railway.app (Бесплатно)

1. Создайте аккаунт на https://railway.app
2. "New Project" → "Deploy from GitHub repo"
3. Выберите репозиторий
4. Добавьте переменные окружения из `.env`
5. Railway автоматически развернет приложение
6. Получите публичный URL
7. Обновите в BotFather

#### Вариант 3: Vercel (Только для статики)

Для Vercel нужен отдельный backend на другом хостинге, так как Vercel не поддерживает long-running процессы.

### После деплоя:

1. Обновите `APP_URL` в переменных окружения хостинга
2. Обновите Web App URL в BotFather:
   ```
   /myapps → Выберите приложение → Edit Web App URL
   ```
3. Протестируйте бота в Telegram

---

## 5. Решение проблем

### Проблема: "Cannot find module"

**Решение:**
```powershell
Remove-Item node_modules -Recurse -Force
npm install
```

### Проблема: Порт 3000 уже занят

**Решение:** Измените порт в `.env`:
```
PORT=3001
```

### Проблема: База данных не создается

**Решение:** Проверьте права доступа к папке `backend/`

### Проблема: Бот не отвечает

**Решение:** Проверьте:
1. Правильность токена в `.env`
2. Что бот запущен (@BotFather → /mybots)
3. Логи в консоли сервера

### Проблема: Приложение не открывается в Telegram

**Решение:**
1. Проверьте, что URL в BotFather правильный
2. URL должен быть HTTPS (кроме localhost)
3. Сервер должен быть запущен и доступен

### Проблема: Ошибка "polling_error"

**Решение:** Возможно, бот уже запущен в другом месте. Остановите все процессы Node.js:
```powershell
Get-Process node | Stop-Process -Force
```

---

## 📊 Структура проекта

```
NVK-Driver/
├── backend/
│   ├── server.js       # Главный серверный файл
│   ├── database.js     # Работа с БД
│   └── database.sqlite # База данных (создается автоматически)
├── public/
│   ├── index.html      # HTML разметка
│   ├── css/
│   │   └── style.css   # Стили с неоновым дизайном
│   └── js/
│       └── app.js      # Логика приложения
├── .env                # Настройки (НЕ коммитить!)
├── .gitignore
├── package.json
└── README.md
```

---

## 🔧 Полезные команды

### Просмотр логов:
```powershell
# Логи выводятся в консоль автоматически
```

### Остановка сервера:
```
Ctrl + C
```

### Очистка базы данных:
```powershell
Remove-Item backend/database.sqlite
# Затем перезапустите сервер
```

### Проверка версии Node.js:
```powershell
node --version
```

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в консоли
2. Убедитесь, что все зависимости установлены
3. Проверьте правильность настроек в `.env`
4. Попробуйте перезапустить сервер

---

## ✅ Чек-лист запуска

- [ ] Node.js установлен (v14+)
- [ ] Зависимости установлены (`npm install`)
- [ ] Файл `.env` настроен с правильным токеном
- [ ] Сервер запускается без ошибок
- [ ] Приложение открывается на `http://localhost:3000`
- [ ] Бот отвечает на команду `/start`
- [ ] Приложение открывается в Telegram

---

**Готово! 🎉 Ваше приложение запущено и работает!**
